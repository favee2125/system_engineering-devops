#!/usr/bin/env bash
# This script lists non-hidden files and shows only the part after the first dash (-) in each filename
for file in *; do
  # Check if it's a file and not a hidden one
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
