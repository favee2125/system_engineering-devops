#!/usr/bin/env bash
# This script lists non-hidden files in the current directory and displays only the part of each filename after the first dash (-)
for file in *; do
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
